# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Searching for the default period
#GET        /v1/records/$id<[0-9]{4}>                    controllers.v1.AdminDataController.lookup(id: String, period: Option[String] ?= None, max: Option[Long] ?= None)
GET        /v1/records/:id                     controllers.v1.AdminDataController.lookup(id: String, period: Option[String] ?= None, max: Option[Long] ?= None)

GET        /v1/records/:id/history/:max        controllers.v1.AdminDataController.lookup(id: String, period: Option[String] ?= None, max: Option[Long])

GET        /v1/records/:id/periods/:period     controllers.v1.AdminDataController.lookup(id: String, period: Option[String], max: Option[Long] ?= None)

# Route for health check information (in JSON).
GET        /health                                      controllers.HealthController.health

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file                                controllers.Assets.at(path="/public", file)
